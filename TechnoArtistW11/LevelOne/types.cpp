#include <iostream>
#include <vector>
#include <map>

void autoType(void) {
	signed short variableShort; // datos numericos enteros con signo (negativos o positivo)
	unsigned int variableInt = 1;   // datos numericos enteros sin signo (positivos)
	double variableDouble = 3.14159; // dato constante

	int vInt;
	vInt = variableInt;

}